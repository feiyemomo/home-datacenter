# Mosquitto ACL file for Home Datacenter
#
# Restricts what each authenticated client may do on the broker.

# ---- API server (home-datacenter) ----
# Also used by Frigate (shares the same account via MQTT_USERNAME/
# MQTT_PASSWORD env vars). Frigate publishes to frigate/# topics
# (topic_prefix: frigate in config.yml), so the ACL must grant
# readwrite on both namespaces.
user home-datacenter
topic readwrite home-datacenter/#
topic readwrite frigate/#

# ---- Default (anonymous / unlisted users) ----
# With allow_anonymous=false, anonymous clients are already rejected
# at the password check; this is a defence-in-depth no-op.
# NOTE: do not add "topic deny #" here. In Mosquitto 2.x a trailing
# anonymous deny rule is treated as a default-deny for all users and
# overrides the per-user ACL entries above.
