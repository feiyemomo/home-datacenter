FROM alpine:3.20
RUN sed -i 's|dl-cdn.alpinelinux.org|mirrors.aliyun.com|g' /etc/apk/repositories \
    && apk add --no-cache ca-certificates tzdata
# Pre-built patched go2rtc binary, with const keepalive raised
# from 5s to 30s (see deploy/go2rtc/Dockerfile for context).
COPY go2rtc /usr/local/bin/go2rtc
COPY go2rtc.yaml /etc/go2rtc.yaml

EXPOSE 1984 8555

ENTRYPOINT ["/usr/local/bin/go2rtc", "-config", "/etc/go2rtc.yaml"]
